@page
@model Zealand_Zoo_1FProjekt1semester2024.Pages.Events.LoginModel
@{
    ViewData["Title"] = "Login";
}
<h2>Login</h2>

<form method="post">
    <div class="form-group">
        <label asp-for="Administrator.Name"></label>
        <input asp-for="Administrator.Name" class="form-control" />
    </div>
    <div class="form-group">
        <label asp-for="Administrator.Password"></label>
        <input asp-for="Administrator.Password" type="password" class="form-control" />
    </div>
    <div class="form-group">
        <button type="submit" class="btn btn-primary">Login</button>
    </div>
</form>

@if (!string.IsNullOrEmpty(Model.Message))
{
    <div class="alert alert-danger">@Model.Message</div>
}